A java spring api to save and retrieve data from a database in azure.
Will serve GET and POST requests to a LotteryApp frontend in react.

### You can run the api locally with the command:
```./mvnw spring-boot:run```
